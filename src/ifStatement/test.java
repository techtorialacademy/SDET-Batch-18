package ifStatement;

public class test {
    public static void main(String[] args) {
       short s= 5;
       byte b = 9;
       short c =(short) (b + s);
       // Note : When data types that are smaller than integer
        // gets in to math operation they will result in int type.
    }
}
